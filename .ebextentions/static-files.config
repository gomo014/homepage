option_settings:
aws:elasticbeanstalk:environment:proxy:staticfiles:
  /static: staticfiles

container_commands:
  01_setup_node:
    command: |
      rm -rf /tmp/deployment/static
      ln -s $EB_APP_DEPLOY_DIR/staticfiles /tmp/deployment/static